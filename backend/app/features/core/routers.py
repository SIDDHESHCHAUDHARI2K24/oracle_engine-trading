"""Core feature router placeholder.

The `core` feature is infrastructure-only and intentionally does not
expose any public API routes. This module is kept as a placeholder
to make the feature layout consistent with other features that
do define routers.
"""
